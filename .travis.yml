language: cpp

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y cdbs fakeroot

script: dpkg-buildpackage -us -uc -rfakeroot

deploy:
  provider: releases
  api_key: 
    secure: "4c35e8dd886da61345f6c47dc2054a53291a0615"
  file_glob: true
  overwrite: true
  draft: true
  file: 
    - "../libpluxx*.deb"
    - "../libpluxx*.tar.gz"
  skip_cleanup: true
  on:
    tags: false
