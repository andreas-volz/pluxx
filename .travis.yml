language: cpp

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y cdbs fakeroot

script: dpkg-buildpackage -us -uc -rfakeroot

deploy:
  provider: releases
  api_key: 
    secure: "347936059999ad15aace09fe393d572acca45ed9"
  file_glob: true
  overwrite: true
  draft: true
  file: 
    - "../libpluxx*.deb"
    - "../libpluxx*.tar.gz"
  skip_cleanup: true
  on:
    tags: false
